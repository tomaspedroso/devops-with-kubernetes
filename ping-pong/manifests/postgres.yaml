apiVersion: apps/v1
kind: StatefulSet
metadata:
  namespace: exercises
  name: pingpong-pg-stset
spec:
  serviceName: pingpong-pg-svc
  replicas: 1
  selector:
    matchLabels:
      app: pingpong-pg-app
  template:
    metadata:
      labels:
        app: pingpong-pg-app
    spec:
      containers:
        - name: postgresql-db
          image: postgres:18.4
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: pingpong-pg-configmap
                  key: POSTGRES_USER
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: pingpong-pg-configmap
                  key: POSTGRES_DB
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pingpong-pg-secrets
                  key: POSTGRES_PASSWORD
          ports:
            - name: postgres
              containerPort: 5432
          volumeMounts:
            - name: pingpong-pg-data-storage
              mountPath: /var/lib/postgresql
  volumeClaimTemplates:
    - metadata:
        name: pingpong-pg-data-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-path
        resources:
          requests:
            storage: 100Mi

---
apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: pingpong-pg-svc
spec:
  ports:
    - port: 5432
      name: postgres
  clusterIP: None
  selector:
    app: pingpong-pg-app
